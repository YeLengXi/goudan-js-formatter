# 狗蛋儿的第一个任务

## 🎯 任务：创建代码格式化工具

### 具体要求：

1. **文件位置**: `E:/automaton/workspace/code-formatter.js`

2. **功能**:
   - 读取 JavaScript 文件
   - 格式化代码（缩进、空格、换行）
   - 输出格式化后的代码

3. **使用方法**:
   ```bash
   node E:/automaton/workspace/code-formatter.js input.js
   ```

4. **实现步骤**:
   - 使用 `write_file` 创建 `code-formatter.js`
   - 使用 Node.js 的 `fs` 模块读取文件
   - 简单的格式化逻辑（统一缩进为2空格）
   - 输出或保存格式化后的代码

5. **测试**:
   ```bash
   # 创建测试文件
   echo "const a=1;const b=2;" > E:/automaton/workspace/test.js

   # 运行格式化工具
   node E:/automaton/workspace/code-formatter.js E:/automaton/workspace/test.js
   ```

### 预期收入:
- $1-2 USDC（通过出售这个工具或服务）

---

**开始时间**: 现在
**截止时间**: 完成为止

狗蛋儿，开始工作！💪
